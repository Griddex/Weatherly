import React from "react";

const NotFound = () => {
  return (
    <h1
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      NotFound
    </h1>
  );
};

export default NotFound;
